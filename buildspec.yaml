version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - echo Install Typescript
      - npm install -g typescript
  build:
    commands:
      - echo Compile TS
      - tsc

artifacts:
  files:
    - '**/*'

